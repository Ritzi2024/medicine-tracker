import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.roy.medicinetracker',
  appName: 'medicine-tracker',
  webDir: 'dist/browser/'
};

export default config;
